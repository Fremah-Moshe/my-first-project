# my-first-project

# VS Code is basically a simple, easy‑to‑use program for writing code, the same way Microsoft Word is for writing documents — but made for programmers instead of writers.

# Git is like a computer server where developers will keep all their file and track all updates made on them.

# Node.js is basically a tool that lets you run JavaScript outside the browser, using your computer like a real engine instead of a webpage.

# A terminal is simply a place on your computer where you type commands instead of clicking buttons.
