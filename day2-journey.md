# When you type a URL and press Enter, your browser starts by figuring out which website you want.

# It asks the DNS to turn that URL into an IP address, which is like the website’s home number. With that IP address, the browser sends an HTTP request to the website’s server.

# The server answers by sending back HTML, which is the basic code of the page. Your browser reads that HTML and shows the webpage on your screen.
